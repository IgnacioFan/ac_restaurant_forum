<div class="row">
  <div class="col-md-10 mx-auto mt-5 mb-3">
    <div class="row">
      <div class="col-md-4">
        <img src="" class="img-responsive center-block" style="width: 200px; margin: 20px 30px;">
      </div>
      <div class="col-md-8">
        <h5>{{user.name}}</h5>
        <h5>{{user.email}}</h5>
        <a href="/users/{{user.id}}/edit">
          <button type="button" class="btn btn-primary">edit</button>
        </a>
      </div>
    </div>
  </div>
</div>